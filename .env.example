# Server-side proxy target for local development. The browser always uses /api/v1.
API_INTERNAL_URL=http://127.0.0.1:8000
# NEXT_PUBLIC_API_URL is no longer used; do not expose backend URLs or tokens.
